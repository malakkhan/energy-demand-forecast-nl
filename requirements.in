# Core HTTP for VIIRS and KNMI downloads
requests

# HDF5 reading for VIIRS VNP46A2 satellite data
h5py

# Excel reading for ENTSO-E load data
openpyxl

# Data manipulation (used in preprocessing for small-file parsing)
pandas

# Numerical operations (used in VIIRS pixel masking and aggregation)
numpy

# Polygon-based spatial masking for Netherlands boundary (GADM)
shapely

# Distributed data processing pipeline (Phase 2/3)
pyspark

# Parquet I/O for Phase 1 (direct file writing, bypasses JVM overhead)
pyarrow

# ── Analysis notebook dependencies (analysis/01_time_series_analysis.ipynb) ──

# Plotting
matplotlib
seaborn

# Time series analysis: STL decomposition, ACF/PACF, VIF
statsmodels

# Jupyter environment for running notebooks
jupyterlab

# Jupyter kernel registration (registers this venv as a kernel inside JupyterHub)
ipykernel

# ── Baseline model dependencies (src/models/baselines/) ──

# Prophet (Facebook/Meta) for trend + seasonality decomposition
prophet

# TensorFlow/Keras for LSTM (matching the reference implementation)
tensorflow

# scikit-learn for MinMaxScaler, IterativeImputer, and PCA
scikit-learn

# scipy for pearsonr correlation
scipy
